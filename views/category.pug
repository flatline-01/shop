include head.pug
include header-light.pug
main
    section(class='row g-0 category-page-block p-4' style=`background-image: url('data:image/png;base64,${category.sliderImage}');`)
        div(class='col-sm-12 col-md-5 col-lg-5  category-page-block__image text-white')
            img(src=`data:image/jpeg;base64,${category.goods[0].images[Object.keys(category.goods[0].images)[0]]}` class='bike-image' alt=`image of the new bike in the category ${category.name}`)
            img(src=`data:image/png;base64,${category.image}` class='bg-image, category-bg-image' alt='category image')
        div(class='col-sm-12 col-md-5 col-lg-5 offset-xl-1 text-white category-page-block__text')
            h1(class='title') #{category.name.toUpperCase()}
            p(class='text') #{category.description}
    include product_card.pug
include footer.pug
script.
    let categoryGoods = !{JSON.stringify(category.goods)};
include scripts.pug
